version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/nextjs-workspace
    overwrite: yes
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 600
      runas: ec2-user
